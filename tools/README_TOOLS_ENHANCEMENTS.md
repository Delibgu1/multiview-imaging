
# Tools Enhancements

- **tools/cog_convert.ps1**: equivalente PowerShell do `tools/cog_convert.sh` para Windows.
  - Uso: `./tools/cog_convert.ps1 -In input.tif -Out saida_cog.tif`
  - Requer: `pip install rio-cogeo`

(Arquivos existentes no seu `tools/` continuam válidos.)
